#!/usr/bin/env node

/*
 * This file is part of the SQL Report.
 * (MIT) fenec #devs <https://www.fenec.cl>
 * @author Guillermo Quinteros <gquinteros@fenec.cl>
 */
'use strict';

const app = require('../app');
const http = require('http');

const PORT = process.env.PORT || 3000;
const server = http.createServer(app);

server.listen(PORT, () => {
	console.log(`Listening : ${PORT}`);
});
